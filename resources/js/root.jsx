import React from 'react';

class Root extends React.Component {
    constructor(props, ctx) {
        super(props, ctx);
    }

    render() {
        return(
            <div>RootPath</div>
        );
    }
}

export default Root;
