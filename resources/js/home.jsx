import React from 'react';

class Home extends React.Component {
    constructor(props, ctx) {
        super(props, ctx);
    }

    render() {
        return(
            <div>HomePath</div>
        );
    }
}

export default Home;
